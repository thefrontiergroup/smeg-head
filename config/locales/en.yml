# Sample localization file for English. Add more files in this directory for other locales.
# See http://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  activerecord:
    errors:
      messages:
        invalid_key_contents: 'does not contain a valid ssh key'
        can_not_be_changed: 'can not be changed'
        invalid_key_algorithm: 'contains an invalid key algorithm (must be ssh-rsa or ssh-dss)'
        does_not_look_like_a_key: 'does not look like a valid ssh key'
        unable_to_find_user: 'is not a registered user name'